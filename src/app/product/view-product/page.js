export default function viewPage({searchParams}){
    console.log(searchParams.random, searchParams.search);
    return (
        <div>
            <h1>this is viewPage under the product page</h1>
        </div>
    )
}